import 'package:flutter/material.dart';

class AppColors {
  // 🔥 Baggrund (lidt lysere end før)
  static const background = Color(0xFF111820);

  // Panels (mere glass / mindre hvidt)
  static const panel = Color(0x14FFFFFF);
  static const panelSoft = Color(0x0FFFFFFF);

  // Tekst (bedre kontrast)
  static const text = Color(0xFFF5F7FA);
  static const muted = Color(0xFFBFC7D5);

  // 🔥 Guld (mere premium)
  static const gold = Color(0xFFF0D07A);
  static const gold2 = Color(0xFFC9A54E);

  // Tekst blå (mere clean)
  static const blueText = Color(0xFFDCE6FF);
  static const blueText2 = Color(0xFFF0F4FF);

  // Linjer / borders
  static const line = Color(0x26FFFFFF);
}