import 'package:flutter/material.dart';

class AppColors {
  // Baggrund
  static const background = Color(0xFFFAF8F3);

  // Panels
  static const panel = Color(0xFFFFFFFF);
  static const panelSoft = Color(0xFFF8F5EF);

  // Tekst
  static const text = Color(0xFF183B32);
  static const muted = Color(0xFF6E6558);

  // Guld
  static const gold = Color(0xFFCAA25A);
  static const gold2 = Color(0xFF9F7A32);

  // Tekst grøn / premium
  static const blueText = Color(0xFF183B32);
  static const blueText2 = Color(0xFF2F302C);

  // Linjer / borders
  static const line = Color(0xFFE8E1D5);
}